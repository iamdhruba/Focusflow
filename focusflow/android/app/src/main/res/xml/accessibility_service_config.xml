<?xml version="1.0" encoding="utf-8"?>
<!--
  Phase 1 (per-screen blocking): added typeWindowContentChanged so the
  AccessibilityService fires on in-app tab swaps, not just app switches.
  Single-activity social apps (Instagram, TikTok) reuse the same Activity
  when navigating between Home and Reels/FYP, so typeWindowStateChanged
  alone is insufficient. The walker inside FocusAccessibilityService
  debounces walks to ≤1 per 500ms so the high event volume stays cheap.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
